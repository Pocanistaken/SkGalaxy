import:
	java.io.File
	org.apache.commons.io.FileUtils 
	
effect backup folder %string% to %string%:
    trigger:
        FileUtils.copyDirectory(new File(expression-1) and new File(expression-2))